import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '3d_pam2_2021',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
